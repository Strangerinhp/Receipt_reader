# Must satisfy the SQL Server password policy: 8+ characters and 3 character classes.
MSSQL_SA_PASSWORD=ChangeThis_StrongPassw0rd!

# Host ports. SQL Server uses 14330 to avoid clashing with a host installation.
FRONTEND_PORT=3000
BACKEND_PORT=5000
MSSQL_HOST_PORT=14330
MAX_UPLOAD_MB=25
